# Design and contract

The full design lives in the team document tree under `tech/micro-connector/`:

| | |
|---|---|
| `01-split-assessment-mt.md` | what to split, in what order, what must not break |
| `02-ccproxy-consumer-notes.md` | the second consumer's requirements and decisions |
| `03-architecture.md` | four layers and the one seam (the Bus) |
| `04-screen-engine.md` | the shared engine and per-program declarations — the expensive part |
| `05-contract.md` | enroll, the screen/Bus message set, applet primitives, compatibility rules |
| `06-repo-and-testbed.md` | repository layout, the reference control plane, what CI must cover |
| `07-roadmap.md` | three steps, each of which can be stopped at |

Those documents will be mirrored here as the code catches up with them.
