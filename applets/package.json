{
  "name": "@micro-teams/connector-applets",
  "version": "0.0.1",
  "private": false,
  "license": "MIT",
  "description": "Screen engine and program declarations (Claude Code, Codex) for micro-connector",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json",
    "test": "npm run build && node build-test.mjs && node --test \"dist-test/*.test.js\"",
    "check": "npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  }
}